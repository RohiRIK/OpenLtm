node_modules/
dist/
*.bundle.mjs
!hooks/GitCommit.bundle.mjs
graph-app/.next/
graph-app/node_modules/
graph-app/test-results/
graph-app/tsconfig.tsbuildinfo

# Rename scratch workspace — local only, contains DB backup (never commit)
.rename/

# User data — never commit
data/openltm.db
data/openltm.db-shm
data/openltm.db-wal
data/*.bak*

# Env
.env
.env.local
# Orchestration artifacts
.sisyphus/
specs/
node-compile-cache/
*.tmp

# Per-project plugin settings (never commit)
.claude/*.local.md
.claude/*.local.json
.dev-team/

# Aegis runtime (auto-added)
.aegis/
.antigravitycli/


# macOS
.DS_Store

# Local dev tooling (Aegis / OpenCode) — not for public repo
aegis-policy.json
opencode.json
.opencode/
